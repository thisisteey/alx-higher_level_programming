#!/usr/bin/python3
# match bytecode according to smaple provided
# using def magic_calculation(a, b, c):

def magic_calculation(a, b, c):
    if a < b:
        return (c)
    elif c > b:
        return (a + b)
    else:
        return (a * b - c)
